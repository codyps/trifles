#include <stdilib.h>

static size_t best_pos(char *str, size_t len)
{
	uint8_t score[len];
	size_t i;
	for (i = 0; i < len; i++) {
		
	}
}

int shorten_string_to_buf(char *str, size_t len, char *out, size_t out, size_t max_len)
{
	
}
